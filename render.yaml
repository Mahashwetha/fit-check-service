services:
  - type: web
    name: rolescore
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false   # set manually in Render dashboard — never committed to git
